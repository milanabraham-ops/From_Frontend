VITE_API_URL=http://localhost:5000/api

# Google Sign-In — OAuth 2.0 Client ID (Web application) from GCP Console > APIs & Services
# > Credentials. Must match GOOGLE_OAUTH_CLIENT_ID on the backend.
VITE_GOOGLE_CLIENT_ID=
